name: Getting Started

on:

  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Checking in Folder
        run: |
          echo Here is what is in the folder
          ls -la
          
      - name: Running shell script
        run: sh ./github-actions-script.sh
